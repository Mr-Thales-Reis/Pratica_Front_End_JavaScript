export function verificador(){
    if(lista.children.length < 1){
        msg.innerText = "A lista de tarefas está vazia. Adicione itens para começar!"
    }else{
        msg.innerHTML = ""
    }
    
}
